<Window x:Class="TimeTrackerTasks.WPF.Views.AddEditTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить/Редактировать задачу" 
        Height="400" Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Название -->
        <TextBlock Text="Название:*" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10"/>
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="0" Grid.Column="1" Margin="0,0,0,10"/>

        <!-- Описание -->
        <TextBlock Text="Описание:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10"/>
        <TextBox Text="{Binding Description}" 
                 Grid.Row="1" Grid.Column="1" 
                 Height="60" Margin="0,0,0,10"
                 TextWrapping="Wrap" AcceptsReturn="True"/>

        <!-- Срок выполнения -->
        <TextBlock Text="Срок выполнения:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,10"/>
        <DatePicker SelectedDate="{Binding DueDate}" 
                    Grid.Row="2" Grid.Column="1" Margin="0,0,0,10"/>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" 
                    Grid.Row="4" Grid.ColumnSpan="2">
            <Button Content="Сохранить" Click="SaveButton_Click" 
                    IsDefault="True" Margin="0,0,10,0" Padding="15,5"/>
            <Button Content="Отмена" Click="CancelButton_Click" 
                    IsCancel="True" Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>